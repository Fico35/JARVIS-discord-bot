module.exports = {
    name: 'refresh',
    description: 'Refreshes the bot config, commands and logs in again.',
    execute(msg, args) {
        // reload config
        // reload commands
        // re-log
    }
}